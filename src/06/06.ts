export function parse(input: string) {
  return input;
}

export function partOne(input: ReturnType<typeof parse>): number {
  // Pen and paper algebra baby
  return 0;
}

export function partTwo(input: ReturnType<typeof parse>): number {
  return 0;
}
